# Superseded held-out refusal protocol

Earlier versions used this file as a proposed future-work protocol. In v28 the protocol has been executed as a final L1-only held-out refusal stress test.

Use these v28 files instead:

- `data/lc_utt_heldout_refusal.jsonl`
- `data/heldout_refusal_summary.csv`
- `HELDOUT_REFUSAL_STRESS_TEST.md`

The manuscript reports the stress-test result in prose rather than as a standalone table to preserve the 6-page IEEE layout.
